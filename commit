#/bin/bash
git config --global user.name "KamalAbdullayev0"
git config --global user.email "abdullayevkamal16.08@gmail.com"
chmod -R +x .
git add --all
git commit -m "Next task"
git push -u origin main
